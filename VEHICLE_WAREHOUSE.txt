CREATE OR ALTER VIEW [dbo].[VEHICLE_WAREHOUSE]
AS
SELECT BC, WAREHOUSE FROM RMD_TRNPROD P JOIN MENUITEM MI ON P.MCODE = MI.MCODE
WHERE MI.PTYPE = 4
GROUP BY BC, WAREHOUSE
HAVING SUM(REALQTY_IN - REALQTY) > 0


