CREATE OR ALTER PROC RSP_TOPNMEMBERSHIPSALES
@DATE1 DATETIME,
@DATE2 DATETIME,
@DIVISION VARCHAR(3) = '%',
@CHK_OPENING TINYINT = 0
AS
IF @CHK_OPENING = 0
	EXEC MEMBERSHIPREPORT_PROC @DATE1, @DATE2, 4, '', @DIVISION
ELSE 
    EXEC MEMBERSHIPREPORT_WITHOP_PROC @DATE1, @DATE2, 4, '', @DIVISION