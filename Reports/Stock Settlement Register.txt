CREATE OR ALTER PROC  [dbo].[RSP_STOCKSETTLEMNTREGISTER]
	@DATE1 DATETIME,
	@DATE2 DATETIME,
	@SETTLEMENTMODE varchar(25) = '%',
	@DIVISION VARCHAR(15) = '%'
AS
EXEC SP_STOCKSETTLEMNTREGISTER @DATE1, @DATE2, @SETTLEMENTMODE, @DIVISION