IF NOT EXISTS (SELECT * FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'tblBillTender')
CREATE TABLE tblBillTender 
(
	VCHRNO VARCHAR(25) NOT NULL,
	DIVISION CHAR(3) NOT NULL,
	PHISCALID VARCHAR(10) NOT NULL,
	COMPANYID VARCHAR(25) NOT NULL,
	CONSTRAINT PK_tblBillTender PRIMARY KEY (VCHRNO, DIVISION, PHISCALID, COMPANYID),	
)

