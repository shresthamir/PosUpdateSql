IF NOT EXISTS(SELECT * FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'COMBO_GROUP')
CREATE TABLE [dbo].[COMBO_GROUP](
	[MCODE] [VARCHAR](25) NOT NULL,
	[GROUPID] [varchar](50) NOT NULL,
	[Quantity] [int] NOT NULL,
	CONSTRAINT FK_COMBO_GROUP_MENUITEM FOREIGN KEY (MCODE) REFERENCES MENUITEM (MCODE)
) 
