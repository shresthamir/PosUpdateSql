IF NOT EXISTS (SELECT * FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'HTL_BOOKING')
BEGIN
CREATE TABLE [dbo].[HTL_BOOKING](
	[BookingId] [varchar](25) NOT NULL,
	[DIVISION] [varchar](3) NOT NULL,
	[PHISCALID] [varchar](10) NOT NULL,
	[CategoryId] [int] NOT NULL,
	[RoomId] [int] NULL,
	[CUSTID] [varchar](25) NOT NULL,
	[CheckInDate] [datetime] NOT NULL,
	[CheckOutDate] [datetime] NOT NULL,
	[PAX_ADULT] [int] NOT NULL,
	[PAX_CHILDREN] [int] NOT NULL,
	[BookingRate] [decimal](25, 16) NOT NULL,
	[Source] [varchar](50) NOT NULL,
	[USER] [varchar](50) NOT NULL,
	[ADVANCE] [decimal](25, 16) NOT NULL,
	[EntryDate] [datetime] NOT NULL,
	[Remarks] [varchar](500) NULL,
 CONSTRAINT [PK_HTL_BOOKING] PRIMARY KEY CLUSTERED 
(
	[BookingId] ASC,
	[DIVISION] ASC,
	[PHISCALID] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
) ON [PRIMARY]

ALTER TABLE [dbo].[HTL_BOOKING]  WITH CHECK ADD  CONSTRAINT [FK_HTL_BOOKING_HTL_ROOM] FOREIGN KEY([RoomId])
REFERENCES [dbo].[HTL_ROOM] ([RoomId])

ALTER TABLE [dbo].[HTL_BOOKING] CHECK CONSTRAINT [FK_HTL_BOOKING_HTL_ROOM]

ALTER TABLE [dbo].[HTL_BOOKING]  WITH CHECK ADD  CONSTRAINT [FK_HTL_BOOKING_HTL_ROOM_CATEGORY] FOREIGN KEY([CategoryId])
REFERENCES [dbo].[HTL_ROOM_CATEGORY] ([CategoryId])

ALTER TABLE [dbo].[HTL_BOOKING] CHECK CONSTRAINT [FK_HTL_BOOKING_HTL_ROOM_CATEGORY]
END


