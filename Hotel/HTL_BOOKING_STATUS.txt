IF NOT EXISTS (SELECT * FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'HTL_BOOKING_STATUS')
BEGIN
CREATE TABLE [dbo].[HTL_BOOKING_STATUS](
	[BookingId] [varchar](25) NOT NULL,
	[DIVISION] [varchar](3) NOT NULL,
	[PHISCALID] [varchar](10) NOT NULL,
	[STATUS] [varchar](50) NOT NULL,
	[USER] [varchar](50) NOT NULL,
	[EntryDate] [datetime] NOT NULL,
 CONSTRAINT [PK_HTL_BOOKING_STATUS] PRIMARY KEY CLUSTERED 
(
	[BookingId] ASC,
	[DIVISION] ASC,
	[PHISCALID] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
) ON [PRIMARY]


ALTER TABLE [dbo].[HTL_BOOKING_STATUS]  WITH CHECK ADD  CONSTRAINT [FK_HTL_BOOKING_STATUS_HTL_BOOKING] FOREIGN KEY([BookingId], [DIVISION], [PHISCALID])
REFERENCES [dbo].[HTL_BOOKING] ([BookingId], [DIVISION], [PHISCALID])


ALTER TABLE [dbo].[HTL_BOOKING_STATUS] CHECK CONSTRAINT [FK_HTL_BOOKING_STATUS_HTL_BOOKING]
END
