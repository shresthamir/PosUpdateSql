IF NOT EXISTS(SELECT * FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'HTL_ROOM')
BEGIN
CREATE TABLE [dbo].[HTL_ROOM](
	[RoomId] [int] NOT NULL,
	[CategoryId] [int] NOT NULL,
	[RoomNo] [varchar](50) NOT NULL,
	[Capacity] [int] NOT NULL,
	[Bedding] [varchar](100) NOT NULL,
	[Amenities] [varchar](500) NOT NULL,
 CONSTRAINT [PK_HTL_ROOM] PRIMARY KEY CLUSTERED 
(
	[RoomId] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
) ON [PRIMARY]


ALTER TABLE [dbo].[HTL_ROOM]  WITH CHECK ADD  CONSTRAINT [FK_HTL_ROOM_HTL_ROOM_CATEGORY] FOREIGN KEY([CategoryId])
REFERENCES [dbo].[HTL_ROOM_CATEGORY] ([CategoryId])


ALTER TABLE [dbo].[HTL_ROOM] CHECK CONSTRAINT [FK_HTL_ROOM_HTL_ROOM_CATEGORY]

END

