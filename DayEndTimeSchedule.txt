IF NOT EXISTS (SELECT * FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'DayEndTimeSchedule')
CREATE TABLE DayEndTimeSchedule
(
	[DATE] Date NOT NULL,
	[EndTime] TIME NOT NULL,
	[REMARKS] VARCHAR(50),
	CONSTRAINT PK_DayEndTimeSchedule PRIMARY KEY ([DATE])
)