IF NOT EXISTS(SELECT * FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'DISCOUNT_PRESETS')
CREATE TABLE [dbo].[DISCOUNT_PRESETS](
	[PresetId] [int] NOT NULL,
	[PresetName] [varchar](50) NOT NULL,
	[FlatDiscount] [bit] NOT NULL,
	[VatInclusive] [bit] NOT NULL,
	[Discount] [numeric](18, 2) NOT NULL,
	[Fixed] [bit] NOT NULL,
	[SkipRoyaltyCalculation] [bit] NOT NULL,
 CONSTRAINT [PK_DISCOUNT_PRESETS] PRIMARY KEY CLUSTERED 
(
	[PresetId] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
) ON [PRIMARY]


